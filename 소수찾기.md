[처음 작성한 답안]
```
def solution(n):
    answer = 0
    arr2 = []
    count=0
    for i in range(n):
        count=0
        for j in range((i+1)//2+1):
            if (i+1) %(j+1)==0:
                count+=1
            else: 
                count=count
        if count == 1:
            arr2.append(i+1)
        else:
            arr2
            
    answer = len(arr2)
    return answer
```
- 테스트 일부에서 시간초과로 실패하였다.

